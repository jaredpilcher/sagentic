version: '3.8'

services:
  telemetry:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - sagentic_data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/sagentic.db

volumes:
  sagentic_data:
